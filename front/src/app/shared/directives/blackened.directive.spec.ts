import { BlackenedDirective } from './blackened.directive';

describe('BlackenedDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackenedDirective();
    expect(directive).toBeTruthy();
  });
});
